<manifest xmlns:android="http://schemas.android.com/apk/res/android">
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
<uses-permission android:name="android.permission.INTERNET" />
<!-- Necessária pra pedir exceção da otimização de bateria (ver
     LocationService.solicitarIgnorarOtimizacaoBateria) — sem ela, muitos
     fabricantes matam a tarefa de rastreamento em segundo plano mesmo com
     a permissão de localização "sempre" concedida. -->
<uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
<!-- Android 13+ exige essa permissão em runtime pra qualquer notificação,
     inclusive as locais (flutter_local_notifications) usadas para avisar de
     uma recomendação nova chegada em segundo plano, e a notificação
     persistente do rastreamento em primeiro plano abaixo. -->
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<!-- Serviço em primeiro plano (ver LocationTrackingService/
     flutter_foreground_task) — sem isso o rastreamento de posição era só
     uma tarefa periódica do WorkManager (best-effort, sujeita a atraso e
     ao Doze), que o sistema podia parar de rodar ao fechar o app; com o
     serviço em primeiro plano + notificação persistente, o rastreamento
     continua rodando mesmo com o app fechado (só para se o mestre tocar
     "Finalizar" ou o sistema encerrar via "Forçar parada"). -->
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
<uses-permission android:name="android.permission.WAKE_LOCK" />
    <application
        android:label="Atlas v1.0"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Serviço em primeiro plano do rastreamento (ver comentário da
             permissão FOREGROUND_SERVICE_LOCATION acima e
             LocationTrackingService) — declarado pelo próprio
             flutter_foreground_task, precisa só do tipo certo aqui. -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="location"
            android:exported="false" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
